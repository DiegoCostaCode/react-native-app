import { Text } from "react-native";
import { SafeAreaView } from "react-native-safe-area-context";

export default function RotaTesteDois() {
    return (
        <SafeAreaView>
            <Text>rota 2</Text>
        </SafeAreaView>
    );
}